# Phase 15.6 — Adoption UX

هدف این فاز این است که کاربر در چند ثانیه بفهمد «چرا باید هر روز به این سیستم برگردد».

## تغییرات اصلی

- Dashboard فروشگاه اکنون ارزش واقعی شبکه را نشان می‌دهد: فروش ۳۰ روز شبکه، تعداد فروش نهایی، تعداد درخواست‌ها، کالاهای منتشرشده و درخواست‌های باز.
- Action Center مواردی را که همین الان نیاز به تصمیم دارند یکجا می‌آورد: رزرو مشتری، درخواست خرید از فروشگاه، کمبود موجودی و خریدهای شبکه در جریان.
- Import Center در `/store/import` برای مهاجرت از نرم‌افزار قبلی اضافه شده است. ورودی فعلی CSV UTF-8 است (Excel → Save As CSV UTF-8).
- Import موجودی زنده قبلی را بازنویسی نمی‌کند. فقط موجودی کالایی که هنوز گردش/رزرو ندارد به‌عنوان موجودی افتتاحیه مقداردهی می‌شود.
- موجودی افتتاحیه با Journal متوازن `INVENTORY / OPENING_BALANCE` ثبت می‌شود.
- صفحه مکانیک خودرو و جست‌وجوهای اخیر را در مرورگر ذخیره می‌کند تا جست‌وجوی روزانه یک‌مرحله‌ای شود.

## اجرا

```bash
./apply.sh --repo ../autoparts-core/
cd ../autoparts-core
docker compose up -d --build
```

Volume PostgreSQL را حذف نکن. migration جدید به‌صورت عادی روی volume فعلی اجرا می‌شود.

## Smoke test

1. با owner وارد `/store` شو و کارت «شبکه برای فروشگاه شما» و Action Center را ببین.
2. وارد `/store/import` شو و فایل نمونه را دانلود کن.
3. یک CSV کوچک ۲ یا ۳ ردیفی Import کن.
4. بررسی کن کالاها در `/store/inventory` دیده شوند و قیمت‌های دارای selling_price در `/store/network` قابل مدیریت باشند.
5. همان فایل را دوباره Import کن؛ کالاهای قبلی باید update شوند و موجودی دارای سابقه نباید بازنویسی شود.
6. با mechanic وارد `/mechanic` شو، خودرو را انتخاب و ذخیره کن، صفحه را reload کن و بررسی کن خودرو خودکار فعال شود.
7. یک جست‌وجو انجام بده؛ عبارت باید در «جست‌وجوهای اخیر» باقی بماند.
8. `make rc-static-check`, `make rc-smoke`, `make rc-db-check` را اجرا کن.

## Migration

آخرین migration:

```text
013_adoption_imports.sql
```
