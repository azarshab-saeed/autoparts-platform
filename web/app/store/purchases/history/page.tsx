"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { useAuth } from "@/components/auth-provider";
import { getPurchasesHistory } from "@/lib/api";
import { downloadCSV } from "@/lib/csv";
import type { PurchaseHistoryItem } from "@/lib/types";
const money=(v:number)=>new Intl.NumberFormat("fa-IR").format(v)+" تومان";
const iso=(d:Date)=>d.toISOString().slice(0,10);const monthStart=()=>{const d=new Date();return iso(new Date(d.getFullYear(),d.getMonth(),1));};
export default function PurchasesHistory(){
 const {session}=useAuth();const[from,setFrom]=useState(monthStart());const[to,setTo]=useState(iso(new Date()));const[q,setQ]=useState("");const[payment,setPayment]=useState<"all"|"paid"|"due">("all");const[items,setItems]=useState<PurchaseHistoryItem[]>([]);const[total,setTotal]=useState(0);const[next,setNext]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");
 async function load(cursor="",append=false){if(!session)return;setLoading(true);setError("");try{const out=await getPurchasesHistory(session,{from,to,q,paymentState:payment,cursor,limit:50});setItems(v=>append?[...v,...out.items]:out.items);setTotal(out.total);setNext(out.next_cursor);}catch(e){setError(e instanceof Error?e.message:"تاریخچه خرید دریافت نشد")}finally{setLoading(false)}}
 useEffect(()=>{const t=setTimeout(()=>void load(),180);return()=>clearTimeout(t)},[session,from,to,q,payment]);
 function exportCSV(){downloadCSV(`purchases-${from}-${to}.csv`,[["شناسه","تاریخ","تأمین‌کننده","مبلغ","پرداخت‌شده","بدهی","تعداد ردیف","تعداد کل"],...items.map(x=>[x.id,x.created_at,x.supplier_name,x.total_amount,x.paid_amount,x.due_amount,x.line_count,x.total_qty])]);}
 return <div><div className="page-head"><div><span className="eyebrow">عملیات خرید</span><h1>تاریخچه خرید</h1><p>سندهای خرید و مانده حساب تأمین‌کنندگان را مرور کن.</p></div><div className="head-actions"><button className="ghost-btn" onClick={exportCSV} disabled={!items.length}>خروجی CSV</button><Link className="primary-btn" href="/store/purchases">+ خرید جدید</Link></div></div>
 <section className="panel operations-filter"><label>از<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>تا<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><label className="grow">جست‌وجو<input value={q} onChange={e=>setQ(e.target.value)} placeholder="نام تأمین‌کننده یا شناسه سند"/></label><label>وضعیت پرداخت<select value={payment} onChange={e=>setPayment(e.target.value as typeof payment)}><option value="all">همه</option><option value="paid">تسویه‌شده</option><option value="due">دارای بدهی</option></select></label></section>
 {error&&<div className="error-box">{error}</div>}<section className="panel history-panel"><div className="panel-head"><div><h2>سندهای خرید</h2><p>{new Intl.NumberFormat("fa-IR").format(total)} سند در بازه انتخابی</p></div></div>{loading&&!items.length?<div className="table-state">در حال دریافت...</div>:!items.length?<div className="table-state">سندی پیدا نشد.</div>:<div className="table-wrap"><table><thead><tr><th>تاریخ</th><th>تأمین‌کننده</th><th>اقلام</th><th>مبلغ</th><th>بدهی</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{new Date(x.created_at).toLocaleDateString("fa-IR")}</b><small className="block-note">{new Date(x.created_at).toLocaleTimeString("fa-IR",{hour:"2-digit",minute:"2-digit"})}</small></td><td>{x.supplier_name}</td><td>{x.line_count} ردیف · {x.total_qty} واحد</td><td>{money(x.total_amount)}</td><td className={x.due_amount>0?"text-danger":"text-success"}>{x.due_amount>0?money(x.due_amount):"تسویه"}</td><td><Link href={`/store/purchases/${x.id}`}>جزئیات</Link></td></tr>)}</tbody></table></div>}{next&&<button className="load-more" disabled={loading} onClick={()=>void load(next,true)}>{loading?"در حال دریافت...":"نمایش بیشتر"}</button>}</section></div>
}
