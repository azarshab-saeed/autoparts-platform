# Phase 15.9 — Checks & Banking

این فاز شکاف اصلی نرم‌افزارهای حسابداری فروشگاهی ایران در حوزه چک و بانک را می‌بندد. همه مبالغ بر حسب تومان هستند و تاریخ چک می‌تواند به شکل شمسی، با ارقام فارسی یا انگلیسی، وارد شود؛ نمونه: `۱۴۰۵/۰۵/۲۷` یا `1405/05/27`.

## اجرا

```bash
docker compose up -d --build
```

PostgreSQL volume را حذف نکن. `db-prepare` migration جدید `015_checks_banking.sql` را روی دیتای موجود اعمال می‌کند.

سپس:

```text
http://localhost:3000/store/checks
```

## تست پیشنهادی دریافتنی

1. یک مشتری با مانده بدهکار انتخاب کن.
2. چک دریافتی با مبلغ کمتر یا مساوی مانده ثبت کن.
3. در `/store/accounts` باید مانده مشتری به همان مبلغ کم شود و گردش «دریافت چک» دیده شود.
4. یک حساب بانکی بساز.
5. چک را «واگذاری به بانک» کن؛ هنوز پول وارد بانک نمی‌شود.
6. «وصول شد» را ثبت کن؛ مانده حساب بانکی باید افزایش یابد.

### برگشتی

یک چک جدید ثبت کن و «برگشتی» بزن. سیستم به‌صورت اتمیک:

- دارایی چک دریافتنی را خارج می‌کند؛
- حساب دریافتنی مشتری را دوباره بدهکار می‌کند؛
- گردش شخص را با `check_bounce` ثبت می‌کند؛
- event و journal متوازن می‌سازد.

## خرج کردن چک

برای چک دریافتنی `در صندوق`، «خرج کردن» را بزن و تأمین‌کننده‌ای با بدهی کافی انتخاب کن. سیستم بدهی تأمین‌کننده را کم و چک دریافتنی را خارج می‌کند. اگر تأمین‌کننده چک را پس داد، `برگشت از تأمین‌کننده` دارایی چک و بدهی تأمین‌کننده را برمی‌گرداند.

## چک پرداختی

مالک/مدیر/حسابدار می‌تواند برای تأمین‌کننده چک پرداختی صادر کند. صدور چک بدهی تأمین‌کننده را کم و `CHEQUES_PAYABLE` را افزایش می‌دهد. هنگام پاس‌شدن، حساب چک پرداختنی بدهکار و حساب بانکی انتخاب‌شده بستانکار می‌شود.

## کنترل‌ها

```bash
./ops/rc-static-check.sh
make rc-db-check
```

DB gate علاوه بر تراز همه journalها، scope شخص/بانک، metadata وضعیت وصول/واگذاری و GL حساب بانکی را بررسی می‌کند.
